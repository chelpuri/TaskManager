server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver #in prod user postgres
    url: jdbc:h2:file:./todos;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true